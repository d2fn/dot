require("toggleterm").setup{
  -- open_mapping = [[<C-/>]],   -- ctrl-/
  direction = "float",        -- popup instead of split
  shade_terminals = true,
  float_opts = {
    border = "curved",
  },
}

